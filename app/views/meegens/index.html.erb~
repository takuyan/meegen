<div id="show_new_meegen" class="corner_round clearfix">
  <%= link_to image_tag("/images/pngs/add.png", :size => "60x60", :id => "show_new_meegen_img"), new_meegen_path, :class => "left" %>
  <span>名言を登録する</span>
</div>
<%= render :partial => 'new' %>
<% if @meegens && @meegens.size > 0 -%>
  <ul id="meegen_list">
  <% @meegens.each do |meegen| %>
    <li class="corner_round">
      <p class="content">
        <%=hbr meegen.name %>
        <%=h meegen.tag_list %>
        <%=h meegen.comments %>
      </p>
      <p class="ctrl_panel">
        <%= link_to 'Show', meegen %>
        <%= link_to 'Edit', edit_meegen_path(meegen) %>
        <%= link_to 'Destroy', meegen, :confirm => 'Are you sure?', :method => :delete %>
      </p>
    </li>
  <% end -%>
  </ul>
<% end -%>
