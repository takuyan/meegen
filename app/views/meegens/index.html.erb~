<div id="tabs">
  <ul>
    <li><a href="#tab-1">名言を見る</a></li>
    <li><a href="#tab-2">あたらしく作成する</a></li>
  </ul>
  <div id="tab-1">
    <div id="show_new_meegen" class="corner_round clearfix">
      <%= link_to image_tag("/images/pngs/add.png", :size => "60x60"), new_meegen_path, :class => "left" %>
      <span>あたらしく作成する</span>
    </div>
    <%= render :partial => 'new' %>
    <% if @meegens && @meegens.size > 0 -%>
      <ul id="meegen_list">
      <% @meegens.each do |meegen| %>
        <li class="corner_round">
          <p class="contant">
            <%=hbr meegen.name %>
            <%=h meegen.tag_list %>
            <%=h meegen.comments %>
          </p>
          <p class="ctrl_panel">
            <%= link_to 'Show', meegen %>
            <%= link_to 'Edit', edit_meegen_path(meegen) %>
            <%= link_to 'Destroy', meegen, :confirm => 'Are you sure?', :method => :delete %>
          </p>
        </li>
      <% end -%>
      </ul>
    <% end -%>
  </div>
  <div id="tab-2">
    aaaaa
  </div>
</div>
