<div id="meegen_show" class="corner_round">
  
  <div id="meegen_name">
    <h1>
      <%=hbr @meegen.name %>
    </h1>
  </div>

  <div id="meegen_tags">
    <%=h @meegen.tag_list %>
    <% form_tag "/meegens/add_tag" do -%>
      <%= label_tag :tag %>
      <%= text_field_tag :tag %>
      <%= hidden_field_tag "meegen_id", @meegen.id %>
      <%= submit_tag "add tag" %>
    <% end -%>
  </div>

  <%= link_to 'Edit', edit_meegen_path(@meegen) %> |
  <%= link_to 'Back', meegens_path %>

  <% @comment = Comment.new -%>
  <% form_for(@comment) do |f| %>
    <%= f.error_messages %>
    <p>
      <%= f.label :name %><br />
      <%= f.text_field :name %>
    </p>
      <%= f.hidden_field :meegen_id, :value => @meegen.id %>
    <!-- p>
      <%= f.label :user_name %><br />
      <%= f.text_field :user_name %>
    </p -->
    <p>
      <%= f.submit 'Create' %>
    </p>
  <% end %>
</div>

