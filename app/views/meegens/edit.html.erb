<h1>Editing meegen</h1>

<% form_for(@meegen) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_area :name %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @meegen %> |
<%= link_to 'Back', meegens_path %>